/**
 * Checkout pricing constants.
 * KSA VAT is provided per-store via store-summary `tax` (typically 15).
 * Keep rates here only when the API does not supply them.
 */
export const CHECKOUT_DEFAULT_TAX_PERCENT = 15;
