/**
 * Default map center used across the app (aligned with zone checks / stores API).
 * Do not use arbitrary Riyadh center — backend rejects points outside active polygons.
 */
export const DEFAULT_SERVICE_LAT = 24.7136;
export const DEFAULT_SERVICE_LNG = 46.6753;
